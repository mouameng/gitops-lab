apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-local
  namespace: argocd

spec:
  secretName: argocd-local-tls

  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer

  dnsNames:
    - argocd.local
