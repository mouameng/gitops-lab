apiVersion: traefik.io/v1alpha1
kind: IngressRoute

metadata:
  name: traefik-external
  namespace: traefik

spec:
  entryPoints:
    - web

  routes:
    - match: Host(`traefik.local`)
      kind: Rule

      services:
        - kind: TraefikService
          name: api@internal
