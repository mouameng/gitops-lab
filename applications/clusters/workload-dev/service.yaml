apiVersion: v1
kind: Service

metadata:
  name: workload-dev
  namespace: traefik

spec:
  type: ExternalName
  externalName: ingress.workload-dev.lab.local

  ports:
    - name: http
      port: 80

    - name: https
      port: 443
